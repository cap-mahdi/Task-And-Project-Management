import { Box } from '@mui/material';
import { FC } from 'react';

export const Login: FC = () => {
  return (
    <Box
      sx={{
        backgroundColor: 'primary.main',
      }}
    >
      Login
    </Box>
  );
};
