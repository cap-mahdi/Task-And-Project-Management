export enum UserRole {
  ADMIN,
  USER,
}

export enum WorkspaceRole {
  WORKSPACE_ADMIN,
  WORKSPACE_EDITOR,
  WORKSPACE_MEMBER,
}
export enum ProjectRole {
  PROJECT_ADMIN,
  PROJECT_EDITOR,
  PROJECT_MEMBER,
}

export enum Status {
  OPEN,
  IN_PROGRESS,
  DONE,
}
